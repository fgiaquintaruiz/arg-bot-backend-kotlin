{
  "info": {
    "_postman_id": "a10a18a4-9e2a-4501-a1b9-89b99d3b8cf2",
    "name": "B Test",
    "description": "Colección para probar la Binance Spot Testnet. Requiere el environment 'Binance Testnet' con API_KEY y API_SECRET.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json",
    "_exporter_id": "30484623"
  },
  "item": [
    {
      "name": "1. Balances de cuenta",
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "exec": [
              "const ts = Date.now().toString();",
              "const qs = 'timestamp=' + ts;",
              "const sig = CryptoJS.HmacSHA256(qs, pm.environment.get('API_SECRET')).toString();",
              "pm.variables.set('timestamp', ts);",
              "pm.variables.set('signature', sig);"
            ],
            "type": "text/javascript"
          }
        }
      ],
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "X-MBX-APIKEY",
            "value": "{{API_KEY}}"
          }
        ],
        "url": {
          "raw": "{{BASE_URL}}/api/v3/account?timestamp={{timestamp}}&signature={{signature}}",
          "host": [
            "{{BASE_URL}}"
          ],
          "path": [
            "api",
            "v3",
            "account"
          ],
          "query": [
            {
              "key": "timestamp",
              "value": "{{timestamp}}"
            },
            {
              "key": "signature",
              "value": "{{signature}}"
            }
          ]
        }
      },
      "response": []
    },
    {
      "name": "2. Precio EUR/USDC (sin firma)",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{BASE_URL}}/api/v3/ticker/price?symbol=EURUSDC",
          "host": [
            "{{BASE_URL}}"
          ],
          "path": [
            "api",
            "v3",
            "ticker",
            "price"
          ],
          "query": [
            {
              "key": "symbol",
              "value": "EURUSDC"
            }
          ]
        }
      },
      "response": []
    },
    {
      "name": "3. Comprar USDC con BTC (market order)",
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "exec": [
              "const ts = Date.now().toString();",
              "const qty = pm.collectionVariables.get('QUANTITY_BTC');",
              "const qs = 'symbol=BTCUSDC&side=SELL&type=MARKET&quantity=' + qty + '&timestamp=' + ts;",
              "const sig = CryptoJS.HmacSHA256(qs, pm.environment.get('API_SECRET')).toString();",
              "pm.variables.set('timestamp', ts);",
              "pm.variables.set('signature', sig);"
            ],
            "type": "text/javascript",
            "packages": {},
            "requests": {}
          }
        },
        {
          "listen": "test",
          "script": {
            "exec": [],
            "type": "text/javascript"
          }
        }
      ],
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "X-MBX-APIKEY",
            "value": "{{API_KEY}}"
          }
        ],
        "url": {
          "raw": "{{BASE_URL}}/api/v3/order?symbol=BTCUSDC&side=SELL&type=MARKET&quantity={{QUANTITY_BTC}}&timestamp={{timestamp}}&signature={{signature}}",
          "host": [
            "{{BASE_URL}}"
          ],
          "path": [
            "api",
            "v3",
            "order"
          ],
          "query": [
            {
              "key": "symbol",
              "value": "BTCUSDC"
            },
            {
              "key": "side",
              "value": "SELL"
            },
            {
              "key": "type",
              "value": "MARKET"
            },
            {
              "key": "quantity",
              "value": "{{QUANTITY_BTC}}",
              "description": "BTC a vender. Configurá QUANTITY_BTC en las variables de la colección."
            },
            {
              "key": "timestamp",
              "value": "{{timestamp}}"
            },
            {
              "key": "signature",
              "value": "{{signature}}"
            }
          ]
        }
      },
      "response": []
    },
    {
      "name": "5. Comprar EUR con USDC",
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "exec": [
              "const ts = Date.now().toString();",
              "const qty = pm.collectionVariables.get('QUOTE_ORDER_QTY');",
              "const qs = 'symbol=EURUSDC&side=BUY&type=MARKET&quoteOrderQty=' + qty + '&timestamp=' + ts;",
              "const sig = CryptoJS.HmacSHA256(qs, pm.environment.get('API_SECRET')).toString();",
              "pm.variables.set('timestamp', ts);",
              "pm.variables.set('signature', sig);"
            ],
            "type": "text/javascript"
          }
        }
      ],
      "request": {
        "method": "POST",
        "header": [{ "key": "X-MBX-APIKEY", "value": "{{API_KEY}}" }],
        "url": {
          "raw": "{{BASE_URL}}/api/v3/order?symbol=EURUSDC&side=BUY&type=MARKET&quoteOrderQty={{QUOTE_ORDER_QTY}}&timestamp={{timestamp}}&signature={{signature}}",
          "host": ["{{BASE_URL}}"],
          "path": ["api", "v3", "order"],
          "query": [
            { "key": "symbol",        "value": "EURUSDC" },
            { "key": "side",          "value": "BUY" },
            { "key": "type",          "value": "MARKET" },
            { "key": "quoteOrderQty", "value": "{{QUOTE_ORDER_QTY}}", "description": "USDC a gastar. Configurá QUOTE_ORDER_QTY en las variables de la colección." },
            { "key": "timestamp",     "value": "{{timestamp}}" },
            { "key": "signature",     "value": "{{signature}}" }
          ]
        }
      },
      "response": []
    },
    {
      "name": "4. Mis ordenes activas",
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "exec": [
              "const ts = Date.now().toString();",
              "const qs = 'timestamp=' + ts;",
              "const sig = CryptoJS.HmacSHA256(qs, pm.environment.get('API_SECRET')).toString();",
              "pm.variables.set('timestamp', ts);",
              "pm.variables.set('signature', sig);"
            ],
            "type": "text/javascript"
          }
        }
      ],
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "X-MBX-APIKEY",
            "value": "{{API_KEY}}"
          }
        ],
        "url": {
          "raw": "{{BASE_URL}}/api/v3/openOrders?timestamp={{timestamp}}&signature={{signature}}",
          "host": [
            "{{BASE_URL}}"
          ],
          "path": [
            "api",
            "v3",
            "openOrders"
          ],
          "query": [
            {
              "key": "timestamp",
              "value": "{{timestamp}}"
            },
            {
              "key": "signature",
              "value": "{{signature}}"
            }
          ]
        }
      },
      "response": []
    }
  ],
  "variable": [
    {
      "key": "BASE_URL",
      "value": "https://testnet.binance.vision"
    },
    {
      "key": "QUOTE_ORDER_QTY",
      "value": "100",
      "description": "USDC a gastar en la order EURUSDC. Ej: 100 = comprar EUR con 100 USDC."
    },
    {
      "key": "QUANTITY_BTC",
      "value": "0.002",
      "description": "BTC a vender en la order BTCUSDC. Ej: 0.002 BTC."
    }
  ]
}